{
  "additionalProperties": false,
  "description": "Stable upstream integration boundary — independent of connector membership edits.",
  "properties": {
    "source_tenant_id": {
      "maxLength": 128,
      "minLength": 1,
      "title": "Source Tenant Id",
      "type": "string"
    },
    "source_product": {
      "maxLength": 64,
      "minLength": 1,
      "title": "Source Product",
      "type": "string"
    },
    "integration_instance_id": {
      "description": "Server-owned upstream integration instance identifier.",
      "maxLength": 128,
      "minLength": 1,
      "title": "Integration Instance Id",
      "type": "string"
    },
    "environment": {
      "anyOf": [
        {
          "maxLength": 64,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Environment"
    },
    "region": {
      "anyOf": [
        {
          "maxLength": 64,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Region"
    }
  },
  "required": [
    "source_tenant_id",
    "source_product",
    "integration_instance_id"
  ],
  "title": "DetectionScopeIdentity",
  "type": "object"
}
