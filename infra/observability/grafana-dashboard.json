{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(shadowtrace_writeback_queue_age_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95 queue age",
          "refId": "A"
        }
      ],
      "title": "写回积压（Outbox queue age p95）",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "percentunit" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(shadowtrace_writeback_total{status=\"confirmed\"}[5m])) / clamp_min(sum(rate(shadowtrace_writeback_total[5m])), 1e-9)",
          "legendFormat": "confirmation rate",
          "refId": "A"
        }
      ],
      "title": "写回确认率",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        {
          "expr": "sum(rate(shadowtrace_writeback_retry_total[5m])) by (adapter)",
          "legendFormat": "{{adapter}} retries",
          "refId": "A"
        },
        {
          "expr": "sum(rate(shadowtrace_writeback_total{status=\"conflict\"}[5m])) by (adapter)",
          "legendFormat": "{{adapter}} conflicts",
          "refId": "B"
        }
      ],
      "title": "写回重试与冲突",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "targets": [
        {
          "expr": "sum(rate(shadowtrace_action_unknown_total[5m])) by (adapter)",
          "legendFormat": "{{adapter}} UNKNOWN actions",
          "refId": "A"
        },
        {
          "expr": "sum(rate(shadowtrace_writeback_total{status=\"unknown\"}[5m])) by (adapter)",
          "legendFormat": "{{adapter}} UNKNOWN writebacks",
          "refId": "B"
        }
      ],
      "title": "UNKNOWN 积压",
      "type": "timeseries"
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "tags": ["shadowtrace", "writeback", "ISSUE-092"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timezone": "browser",
  "title": "ShadowTrace Writeback Observability",
  "uid": "shadowtrace-writeback",
  "version": 1
}
