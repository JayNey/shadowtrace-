apiVersion: 1

providers:
  - name: ShadowTrace
    orgId: 1
    folder: ShadowTrace
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
