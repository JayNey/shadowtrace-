"""Contract sources consumed by export scripts (ISSUE-112).

Edit paths:
- Socket.IO envelope schema (canonical): ``app/contracts/socketio/events.schema.json``
- Committed export copies under repo ``contracts/`` are generated; run ``make update-contracts``.
- OpenAPI / model / tool schemas are exported from runtime code, not hand-edited under ``contracts/``.
"""
